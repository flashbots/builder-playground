apiVersion: v1
kind: Service
metadata:
  name: {{ .Name }}
  namespace: {{ .Namespace }}
  labels:
    app: {{ .Name }}
spec:
  ports:
  {{- range .Ports }}
  - port: {{ .Port }}
    name: {{ .Name }}
  {{- end }}
  selector:
    app: {{ .Name }}
---