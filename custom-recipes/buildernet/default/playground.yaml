base: buildernet
description: Deploy a full L1 stack with mev-boost and builder-hub

recipe:
  lighthouse-beacon-node:
    services:
      beacon:
        replace_args:
          - "--target-peers"
          - "1"
          - "--subscribe-all-subnets"
          - "true"
